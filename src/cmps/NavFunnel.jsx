import React from 'react'

// Nav funnel written hard coded as placeholder for now.
export function NavFunnel() {
    return (
        <section className="nav-funnel">
            <nav>
                <a>{'בחירת התרומה > '}</a>
                <a>{'פרטים אישיים > '}</a>
                <a>פרטי התשלום</a>
            </nav>
        </section>
    )
}
